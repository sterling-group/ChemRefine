## Summary

<!-- What does this PR change and why? -->

Closes #

## Checklist

- [ ] Tests added/updated; `pytest --cov=chemrefine --cov-branch` passes (100%)
- [ ] `ruff check` / `ruff format`, `mypy`, `interrogate` clean
- [ ] Docs updated (`mkdocs build --strict` passes) if behaviour/CLI changed
- [ ] `CHANGELOG.md` Unreleased section updated if user-facing
- [ ] Any new workflow step pins its action to a full commit SHA (see CONTRIBUTING)

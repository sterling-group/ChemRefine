# Sequential ORCA Input Configuration File
# Define each step with its specific parameters.
#This workflow reflects using GOAT and refining methods to improve the accuracy
steps:
  # Step 1: Example using GOAT with None sampling (it's the only option for GOAT)
  - step: 1
    calculation_type: "GOAT"           # Options: GOAT, DFT, MLFF, XTB
    sample_type:
      method: "None"              # Options: E_window, Boltzmann_distribution, integer, None
      parameters: {}       #GOAT takes all of the values from the final ensemble.
             

  # Step 2: Example using MLFF with Boltzmann distribution sampling
  - step: 2
    calculation_type: "MLFF"
    sample_type:
      method: "Boltzmann_distribution"
      parameters:
        weight: 99.99

  # Step 3: Using DFT to refine the calculation
  - step: 3
    calculation_type: "DFT"
    sample_type:
      method: "integer"
      parameters:
        num_samples: 10

  # Step 4: Using a high-precision DFT to finalize the calculations on the best energy. 
  - step: 4
    calculation_type: "DFT"
    sample_type:
      method: "integer"
      parameters:
        num_samples: 1 #Number of files to run for this step.

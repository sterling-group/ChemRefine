# Redox properties of dimethylaniline: GOAT screen → DFT → MLIP/DFT charge states.
template_dir: ./templates
scratch_dir: /scratch/
output_dir: ./outputs
executables: { orca: /orca }

charge: 0
multiplicity: 1

# Optional: override the default initial structure (default: template_dir/step1.xyz).
input: ./templates/step1.xyz

steps:
  # GOAT conformer search; keep up to 95 % cumulative Boltzmann weight.
  - step: 1
    operation: goat
    engine: orca
    sample: { method: boltzmann, percent_cumulative: 95 }

  # DFT re-optimisation; keep everything within 10 kcal/mol of the minimum.
  - step: 2
    operation: opt_sp
    engine: orca
    sample: { method: min, window_kcalmol: 10 }

  # Steps 3-8: the -1/0/+1 redox ladder on the MLIP surface, then on DFT.
  - step: 3
    operation: opt_sp
    engine: mlip-extopt
    charge: -1
    multiplicity: 2
    options: { model_name: uma-s-1, task_name: omol, device: cuda }
    sample: { method: min, count: 0 }

  - step: 4
    operation: opt_sp
    engine: mlip-extopt
    charge: 0
    multiplicity: 1
    options: { model_name: uma-s-1, task_name: omol, device: cuda }
    sample: { method: min, count: 0 }

  - step: 5
    operation: opt_sp
    engine: mlip-extopt
    charge: 1
    multiplicity: 2
    options: { model_name: uma-s-1, task_name: omol, device: cuda }
    sample: { method: min, count: 0 }

  - step: 6
    operation: opt_sp
    engine: orca
    charge: -1
    multiplicity: 2
    sample: { method: min, count: 0 }

  - step: 7
    operation: opt_sp
    engine: orca
    charge: 0
    multiplicity: 1
    sample: { method: min, count: 0 }

  - step: 8
    operation: opt_sp
    engine: orca
    charge: 1
    multiplicity: 2
    sample: { method: min, count: 0 }

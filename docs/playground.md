# Playground

This page is a placeholder that exists only for the navigation: after the site build,
the [playground hook](https://github.com/sterling-group/ChemRefine/blob/main/docs/hooks/playground.py)
replaces this URL with the full-screen workflow builder. Seeing this text means the
hook did not run — build the docs with `mkdocs build` from the repository.
